// below is a simple arrow function with one parameter name
const greeting = (name) => {
  // we log the name parameter
  return console.log(name);
};
// this is how to call a function
greeting("ahmad Khan");
